#pragma once
#include <iostream>
#include <cmath>

class Square
{
public:
    // Const & Dest
    Square(float samplerate, float samplerate);
    ~Square();

    void setSamplerate(float samplerate);
private:

};